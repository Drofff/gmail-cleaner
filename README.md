# Gmail Cleaner

A little web-UI tool for marking Gmail e-mails as read.
For me, marking 18,000+ emails took 3 minutes. Simple but super useful :)

All you need is to put a Google OAuth project secret file `client_secret_*.json` to the src/main/resources directory as `client_secret.json`.
